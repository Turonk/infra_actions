name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    
jobs:
  build:
    runs-on: ubuntu latest
    steps:
    - uses: actions/checkout@v2
    
    - name: Run one-liner
      run: echo Hello, world!
    
    - name: Run multi-liner
      run: |
        echo multi
        echo liner

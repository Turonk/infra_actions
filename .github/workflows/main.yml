name: CI

on:
  push:
    branches: [ masetr ]
  pull_request:
    branches: [ master ]
    
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: action/checkout@v2
    - name: Run a one-line script
      run: echo Hellow, world!
    - name: Run a mulri-line script
      run: |
        echo Add other actions to build,
        echo test, adn depliy you project.
